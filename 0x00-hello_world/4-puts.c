#include <stdio.h>

/**
 * main - would carry the function for the phrase 'Programming is like building a multilingual puzzle
 * Return 0, if stopped properly, else non-zero
 */
int main()
{
	puts("\"Programming is like building a multilingual puzzle");
	return 0;
}
